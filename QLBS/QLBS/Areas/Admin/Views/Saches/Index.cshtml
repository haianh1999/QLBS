@model IEnumerable<QLBS.Models.Sach>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TheLoais.MaTheLoai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenSach)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaSach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TheLoais.MaTheLoai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenSach)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaSach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDSach }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDSach }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDSach })
        </td>
    </tr>
}

</table>
